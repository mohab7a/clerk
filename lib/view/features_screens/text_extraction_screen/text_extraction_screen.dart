import 'package:flutter/material.dart';
import '../../../constants.dart';

class TextExtractionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: kBackgroundColor,
    );
  }
}
